if (window.SimpleSlide) {
  new SimpleSlide({
    slide: "quote",
    auto: true,
    time: 5000,
  });

  new SimpleSlide({
    slide: "portifolio",
    auto: true,
    time: 7000,
    nav: true,
  });
}

if (window.SimpleAnime) {
  new SimpleAnime();
}
